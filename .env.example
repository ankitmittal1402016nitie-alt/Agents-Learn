# Environment template for local development (do NOT commit secrets)
# Copy this file to `.env` and fill in your keys for local runs.

# Google Gemini / Generative AI configuration
# Either set an API key or point GOOGLE_APPLICATION_CREDENTIALS to a service account JSON
GOOGLE_API_KEY=AIzaSyBjiCxrLNV-b7qCs-60dosSq3hK_KHNv44
#GOOGLE_APPLICATION_CREDENTIALS=/full/path/to/credentials.json

# Toggle debug stub mode (set to true to avoid live API calls)
DEBUG=false

# Model configuration (names used by the codebase)
GEMINI_PRIMARY_MODEL=gemini-2.5-flash-lite
GEMINI_EMBEDDING_MODEL=models/text-embedding-004
GEMINI_FALLBACK_MODEL=gemini-1.5-flash

# Fallback embedding dimension if provider/model detection fails
EMBEDDING_DIM_FALLBACK=1536

# Optional: OpenAI fallback (used only if Gemini not configured)
#OPENAI_API_KEY=your-openai-key-here

# External search (Google Programmable Search) - optional
GOOGLE_SEARCH_API_KEY=AIzaSyDufnxhPSrh1h1NZvfkM6IgM0xZA3jCa-A
GOOGLE_SEARCH_CX=b0ec8c01ad0264dbe

# Retrieval / RAG tuning
RAG_TOP_K=4
RAG_FETCH_K=24
RAG_USE_MMR=true
RAG_USE_RERANK=false
MAX_TOKENS_CONTEXT=12000

# Web ingestion TTL (days)
WEB_TTL_DAYS=7

# Server port
PORT=8000